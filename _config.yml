# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html
## Source: https://github.com/hexojs/hexo/

# Site
title: zoengkei_blog
subtitle: 'welcome to zoengkei blog \n 目前是陈奕迅专场'
description: 'zoengkei_blog, 记录生活点滴'
keywords: blog
author: zoengkei
language: zh-CN
timezone: ''

# URL
## 关键修改：改为 Apache 服务器的实际访问地址（必改！）
## 示例：如果通过 http://192.168.1.100 访问，就写这个；如果有域名（如 zoengsang.cloud 已解析到服务器），可保留原域名
url: http://zoengsang.cloud  # 👉 替换为实际访问地址（如 http://192.168.1.50 或 http://zoengsang.cloud）
root: /  # 部署在 WWW 根目录，保持为 /（无需修改）
permalink: :year/:month/:day/:title/
permalink_defaults:
pretty_urls:
  trailing_index: true  # 保留 index.html（Apache 兼容更好）
  trailing_html: true   # 保留 .html（避免路径解析问题）

env: production
# Directory（无需修改）
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Writing（无需修改）
new_post_name: :title.md
default_layout: post
titlecase: false
external_link:
  enable: true
  field: site
  exclude: ''
filename_case: 0
render_drafts: false
post_asset_folder: false
relative_link: false  # 关键：保持 false，使用绝对路径加载资源
future: true
syntax_highlighter: highlight.js
highlight:
  line_number: true
  auto_detect: false
  tab_replace: ''
  wrap: true
  hljs: false
prismjs:
  preprocess: true
  line_number: true
  tab_replace: ''

# Home page setting（无需修改）
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# Category & Tag（无需修改）
default_category: uncategorized
category_map:
tag_map:

# Metadata elements（无需修改）
meta_generator: true

# Date / Time format（无需修改）
date_format: YYYY-MM-DD
time_format: HH:mm:ss
updated_option: 'mtime'

# Pagination（无需修改）
per_page: 10
pagination_dir: page

# Include / Exclude file(s)（无需修改）
include:
exclude:
ignore:

# Extensions（无需修改）
theme: butterfly

# Deployment（Git 部署配置可保留，不影响 Apache 部署）
deploy:
  type: git
  repo:
    github: git@github.com:nidenuoyan/nidenuoyan.github.io.git
    #server: administrator@119.29.128.6:/e/hexo-staging.git
  branch: main